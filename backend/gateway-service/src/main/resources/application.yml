server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: identity-service
          uri: ${IDENTITY_SERVICE_URL:lb://identity-service}
          predicates:
            - Path=/api/auth/**,/api/users/**,/api/tenants/**
        - id: catalog-service
          uri: ${CATALOG_SERVICE_URL:lb://catalog-service}
          predicates:
            - Path=/api/products/**,/api/inventory/**
        - id: order-service
          uri: ${ORDER_SERVICE_URL:lb://order-service}
          predicates:
            - Path=/api/orders/**
        - id: settings-service
          uri: ${SETTINGS_SERVICE_URL:lb://settings-service}
          predicates:
            - Path=/api/settings/**
      globalcors:
        add-to-simple-url-handler-mapping: true
        corsConfigurations:
          '[/**]':
            allowedOrigins: "http://localhost:4200"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders: "*"

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true

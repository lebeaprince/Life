rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: lock down with Firebase Auth + roles.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

